object Reverse extends App
{
  def reverse1(reverse: String): Unit =
  {
  def rev(reverse: String,len:Int, store:String):Unit=
    {
      if(reverse<0)
      store
      else
        println(reverse,len-1,store+reverse.charAt(len))
    }
    println(reverse,reverse.length-1,store="")
}

  }
